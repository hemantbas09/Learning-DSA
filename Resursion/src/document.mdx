A function or method call itself with different parameters and base case to terminate loop;
In recursion we solve bigger problem by solving small problem:

stack overflow:

halting condition or base case:

The recursion spell:
1. Identify smaller problem;
2. self work:
3. Identify base case:
